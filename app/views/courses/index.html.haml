%h1 Listing courses

%table.table
  %thead
    %tr
      %th Title
      %th Description
      %th User
      %th
      %th
      %th

  %tbody
    - @courses.each do |course|
      %tr
        %td= course.title
        %td= course.description
        %td= course.user
        %td= link_to 'Show Details', course
        %td= link_to 'Modify', edit_course_path(course)
        %td= link_to 'Delete', course, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Course', new_course_path

= link_to courses_path, class: 'btn bnt-success' do
  .fa.fa-book
  Course
