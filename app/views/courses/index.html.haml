%h1 Listing courses

.card-columns
  - @courses.each do |course|
    = render 'courses/course', course: course

%table.table
  %thead
    %tr
      %th Title
      %th Description
      %th User
      %th
      %th
      %th

  %tbody
    - @courses.each do |course|
      %tr
        %td= course.title
        %td= course.description
        %td= course.user
        %td= link_to 'Show all Details', course
        %td= link_to 'Change this', edit_course_path(course)
        %td= link_to 'Delete', course, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'Create new Course', new_course_path

= link_to courses_path, class: 'btn bnt-success' do
  .fa.fa-book
  Course
